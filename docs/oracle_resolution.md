# Oracle Resolution

See `oracle_routing_fallback.md` for the canonical fallback mechanism. Explicit Oracle metadata is primary; missing Oracle uses reviewed ID mapping; unknown IDs default safely to semantic LLM evaluation.
