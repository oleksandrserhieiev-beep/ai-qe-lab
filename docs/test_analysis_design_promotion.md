# Promotion boundary

The skeleton stops before mutation. The future promotion phase will take human-approved JSON, run deterministic schema/reference/consistency validation, and only then promote the change into the governed GitHub dataset. Failed post-edit validation must prevent promotion and report exact failures.
