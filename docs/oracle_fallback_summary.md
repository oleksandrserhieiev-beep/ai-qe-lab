# Oracle Fallback Summary

Explicit Oracle metadata is primary. Missing Oracle metadata falls back to the manually reviewed case-ID registry. Unknown IDs default safely to semantic LLM evaluation. The Judge evaluates PASS/FAIL after semantic routing; it does not classify the Oracle type. Deterministic routes require explicit formal assertions and must never be guessed for unknown cases.
