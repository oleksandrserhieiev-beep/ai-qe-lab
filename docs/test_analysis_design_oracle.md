# Oracle assignment

Evaluation proposals explicitly classify the oracle as `deterministic` or `semantic`. Deterministic oracles are preferred when an exact rule can be asserted in Python; semantic oracles are reserved for meaning/quality judgments that cannot be reduced to a stable deterministic assertion. Oracle assignment is part of the reviewable proposal and traceability chain.
