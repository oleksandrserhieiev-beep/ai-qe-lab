# Similarity decision boundary

Semantic similarity is reported with the nearest existing case and a score. It does not automatically mean duplicate. The intended decisions are ADD, EXTEND_EXISTING, or SKIP. EXTEND_EXISTING must show the existing case and proposed extension so the human reviewer can approve the merged form or choose another action.
