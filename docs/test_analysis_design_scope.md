# Skeleton scope boundary

This PR establishes contracts and architecture, not full dataset write-back.

Included now: health/error-warning model, proposal actions, traceability contract, oracle/target contracts, similarity representation, shared cache fingerprint, prompt skeleton, workflow skeleton, and deterministic tests.

Deferred: semantic LLM execution, final Jira eligibility label, GitHub interactive approval/write-back, JSON branch/PR mutation, post-edit promotion, and automatic multi-agent orchestration.
